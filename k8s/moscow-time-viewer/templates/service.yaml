apiVersion: v1
kind: Service
metadata:
  name: {{ include "moscow-time-viewer.fullname" . }}
  labels:
    {{- include "moscow-time-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moscow-time-viewer.selectorLabels" . | nindent 4 }}
