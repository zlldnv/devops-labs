apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscow-time-viewer.fullname" . }}-test-connection"
  labels:
    {{- include "moscow-time-viewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscow-time-viewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
